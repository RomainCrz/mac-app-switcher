# MacAppSwitcher

Minimal native macOS floating app switcher prototype built with SwiftPM, AppKit, and SwiftUI.

The compact bar stays visible at the top-center of the main screen.

## Run

```bash
swift run MacAppSwitcher
```

## Build

```bash
swift build
```
